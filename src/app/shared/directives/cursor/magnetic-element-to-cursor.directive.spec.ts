import { MagneticElementToCursorDirective } from './magnetic-element-to-cursor.directive';

describe('MagneticElementToCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new MagneticElementToCursorDirective();
    expect(directive).toBeTruthy();
  });
});
