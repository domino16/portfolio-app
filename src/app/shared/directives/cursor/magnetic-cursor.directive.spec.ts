import { MagneticCursorDirective } from './magnetic-cursor.directive';

describe('MagneticCursorDirective', () => {
  it('should create an instance', () => {
    const directive = new MagneticCursorDirective();
    expect(directive).toBeTruthy();
  });
});
