import { HeadingScrollDirective } from './heading-scroll.directive';

describe('HeadingScrollDirective', () => {
  it('should create an instance', () => {
    const directive = new HeadingScrollDirective();
    expect(directive).toBeTruthy();
  });
});
