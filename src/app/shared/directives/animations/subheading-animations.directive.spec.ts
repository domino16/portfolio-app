import { SubheadingAnimationsDirective } from './subheading-animations.directive';

describe('SubheadingAnimationsDirective', () => {
  it('should create an instance', () => {
    const directive = new SubheadingAnimationsDirective();
    expect(directive).toBeTruthy();
  });
});
