import { ScrollImageDirective } from './scroll-image.directive';

describe('ScrollImageDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollImageDirective();
    expect(directive).toBeTruthy();
  });
});
